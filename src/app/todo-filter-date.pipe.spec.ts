import { TodoFilterDatePipe } from './todo-filter-date.pipe';

describe('TodoFilterDatePipe', () => {
  it('create an instance', () => {
    const pipe = new TodoFilterDatePipe();
    expect(pipe).toBeTruthy();
  });
});
